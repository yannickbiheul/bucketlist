{% extends './base.html.twig' %}
{% block title %}
	{{ titre }}
{% endblock %}

{% block body %}
    <h1 class='mt-5'>
    {{ titre }}
    </h1>
    <p>{{ wish.description }}</p>
    <p>Le {{ wish.dateCreated | date('d-m-Y') }} à {{ wish.dateCreated | date('H:i') }} par {{ wish.author }}</p>
    <a type="button" href="/form-update/{{ wish.id }}" class="btn btn-primary">Modifier</a>
{% endblock %}
