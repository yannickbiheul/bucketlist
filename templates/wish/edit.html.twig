{% extends 'base.html.twig' %}

{% block title %}Edit Wish{% endblock %}

{% block body %}

<div class="container">
		<div class="row text-center mt-5">
    <h1>Edit Wish</h1>
		</div>

		<div class="row">
			<div class="col-4 offset-4">
            {{ include('wish/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_wish_index') }}" class="btn btn-primary mt-3">back to list</a>
        {{ include('wish/_delete_form.html.twig') }}
			</div>
		</div>

	</div>
    

    

    
{% endblock %}
