{% extends './base.html.twig' %}
{% block title %}
	{{ titre }}
{% endblock %}

{% block body %}
	<h1 class='mt-5'>
		{{ titre }}
	</h1>

	<div class="container mt-5">
		<div class="row d-flex justify-content-center">
			{% for wish in wishs %}
				<div class="card" style="width: 18rem;margin: 10px;">
					<div class="card-body">
						<h5 class="card-title">{{ wish.title }}</h5>
						<h6 class="card-subtitle mb-2 text-muted">{{ wish.author }}</h6>
						<p class="card-text">{{ wish.description }}</p>
                        <span>Le {{ wish.dateCreated | date("d/m/Y")}}</span>
                            <br>
						<a href="{{ path('app_detail', {'id':wish.id}) }}">Détails</a>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>

{% endblock %}
